- if news.present?
  .related_post
    %h2
      Похожие посты
      %i.fa.fa-thumbs-o-up
    %ul.spost_nav.wow.fadeInDown.animated
      - news.each do |news|
        %li
          .media
            = link_to post_path(news), class: %i(media-left), title: news.title  do
              = image_tag news.main_image(:thumb_small), alt: news.title
            .media-body
              = link_to news.title, post_path(news), class: %i(catg_title), title: news.title